<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

<link rel="stylesheet" href="/css/OrderDetails.css">


{{>userNav}}

<div class="container-fluid mt-5 pt-5">
    <div class="container">

        <div class="d-flex justify-content-between align-items-center py-3">
            <h2 class="h5 mb-0"><a href="#" class="text-muted"></a> Order #{{orderData.orderNo}}</h2>
        </div>

        <div class="row">
            <div class="col-lg-8">

                <div class="card mb-4">
                    <div class="card-body">
                        <div class="mb-3 d-flex justify-content-between">
                            <div>
                                <span class="me-3">Date: {{orderData.orderDate}}</span>
                                <br>
                                <span class="me-3">#Trasaction No:{{orderData.transactionId}}</span>
                                <span class="me-3">Payment method: {{orderData.paymentMethod}}</span>
                                <span class="badge rounded-pill bg-info">Staus: {{orderData.orderStatus}}</span>
                            </div>
                            <div class="d-flex">
                                <a href="/profile/myorders/invoice/{{orderData._id}}">
                                <button class="btn btn-dark  d-none d-lg-block btn-icon-text">
                                    <span class="text">Invoice</span>
                                    <i class="fa-solid fa-receipt"></i>
                                    </button>
                                    </a>
                                <div class="dropdown">
                                    <button class="btn btn-link p-0 text-muted" type="button" data-bs-toggle="dropdown">
                                        <i class="bi bi-three-dots-vertical"></i>
                                    </button>
                                    <ul class="dropdown-menu dropdown-menu-end">
                                        <li><a class="dropdown-item" href="#"><i class="bi bi-pencil"></i> Edit</a></li>
                                        <li><a class="dropdown-item" href="#"><i class="bi bi-printer"></i> Print</a>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                        <table class="table table-borderless">
                            <tbody>
                                {{#each orderData.OrderedItems}}
                                <tr>
                                    <td>
                                        <div class="d-flex mb-2">
                                            <div class="flex-shrink-0">
                                                <img src="/uploads/{{this.image}}" alt
                                                    width="35" class="img-fluid">
                                            </div>
                                            <div class="flex-lg-grow-1 ms-3">
                                                <h6 class="small mb-0"><a href="#" class="text-reset">{{this.productname}}</a>
                                                </h6>
                                                <span class="small">Color: Black</span>
                                            </div>
                                        </div>
                                    </td>
                                    <td>{{this.variantId.quantity}}</td>
                                    <td class="text-end">{{orderData.orderAmount}}</td>
                                </tr>
                                {{/each}}
                            </tbody>
                            <tfoot>
                                <tr>
                                    <td colspan="2">Subtotal</td>
                                    <td class="text-end">₹ {{orderData.subTotal}}</td>
                                </tr>
                                <tr>
                                    <td colspan="2">Shipping</td>
                                    <td class="text-end">0.00</td>
                                </tr>
                                <tr>
                                    <td colspan="2">Discount Code:{{orderData.coupon}}</td>
                                    {{#if orderData.coupon}}
                                    <td class="text-danger text-end">-₹{{subtract orderData.subTotal orderData.orderAmount}}</td>
                                    {{else}}
                                    <td class="text-danger text-end">-₹0.00</td>
                                    {{/if}}
                                </tr>
                                <tr class="fw-bold">
                                    <td colspan="2">TOTAL</td>
                                    <td class="text-end">₹{{orderData.orderAmount}}</td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>

                <div class="card mb-4">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-lg-6">
                                <h3 class="h6">Payment Method</h3>
                                <p>{{orderData.paymentMethod}} <br>
                                    Total: ₹{{orderData.orderAmount}} <span class="badge bg-success rounded-pill">PAID</span></p>
                            </div>
                            <div class="col-lg-6">
                                <h3 class="h6">Billing address</h3>
                                <address>
                            <strong>{{orderData.addressName}}</strong><br>
                            {{orderData.addressLine1}}<br>
                            {{orderData.addressLine2}}<br>
                             {{orderData.addressPincode}} <br>
                             {{orderData.addressMobile}} <br>

                        </address>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">

                <div class="card mb-4">
                    <div class="card-body">
                        <h3 class="h6">Customer Notes</h3>
                        <p> Seal all boxes securely with strong packing tape. Ensure that there are no openings or loose edges that could allow items to shift during shipping.</p>
                    </div>
                </div>
                <div class="card mb-4">

                    <div class="card-body">
                        <h3 class="h6">Shipping Information</h3>
                        <strong>Afsal Courier</strong>
                        <span><a href="#" class="text-decoration-underline" target="_blank">FF1234567890</a> <i
                                class="bi bi-box-arrow-up-right"></i> </span>
                        <hr>
                        <h3 class="h6">Address</h3>
                        <address>
                            <strong>{{orderData.addressName}}</strong><br>
                            {{orderData.addressLine1}}<br>
                            {{orderData.addressLine2}}<br>
                             {{orderData.addressPincode}} <br>
                             {{orderData.addressMobile}} <br>

                        </address>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{{>userFooter}}
<script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script type="text/javascript">

</script>